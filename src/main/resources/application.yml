spring:
  application:
    name: booking-system
  datasource:
    url: jdbc:h2:mem:bookingdb
    username: admin
    password: admin123
  h2:
    console:
      enabled: true
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
    format-sql: true